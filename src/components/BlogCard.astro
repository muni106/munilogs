---
const {post} = Astro.props;
---

<div id="postCard">
    <div>
        <img src={post.frontmatter.image.url} width="100" alt="postImage">
    </div>
    <div>
        <a href={post.url}>{post.frontmatter.title}</a>
    </div>
    
</div>
